ID_PRODUTO			INTEGER		NOT NULL PK
DESCRICAO 			VARCHAR(60)	NOT NULL
PALAVRA_CHAVE			VARCHAR(10)	NOT NULL
UM				CHAR(2)		NOT NULL
MARCA				VARCHAR(30)
ID_GRUPO			SMALLINT	NOT NULL
DESC_GRUPO			VARCHAR(60)	NOT NULL
ID_SUBGRUPO			SMALLINT	NOT NULL
DESC_SUBGRUPO			VARCHAR(60)	NOT NULL
LOCALIZACAO			VARCHAR(30)	
REFERENCIA			VARCHAR(25)
ESTOQUE_MIN			DECIMAL		NOT NULL
ESTOQUE_MAX			DECIMAL 	NOT NULL
SALDO_ATUAL			DECIMAL		NOT NULL
LUCRO_PORC			DECIMAL		NOT NULL
COD_BARRA			VARCHAR(60)	NOT NULL
PRECO				DECIMAL		NOT NULL
DATA_CADASTRO			DATE		NOT NULL