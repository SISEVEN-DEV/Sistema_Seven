ID_PAGAMENTO_ORCAMENTO			INTEGER NOT NULL PK AI
ID_PAGAMENTO				SMALLINT NOT NULL FK
DESCRICAO_FORMA_PAGAMENTO		VARCHAR(60) NOT NULL
VALOR_PAGO				DECIMAL(15,2) NOT NULL
ID_ORCAMENTO				INTEGER NOT NULL FK
TIPO_FORMA_PAGAMENTO			VARCHAR(13) NOT NULL
ORDEM					CHAR(1) NOT NULL



CREATE GENERATOR GEN_PAGAMENTO_ORCAMENTO_ID;

CREATE TABLE PAGAMENTO_ORCAMENTO (
    ID_PAGAMENTO_ORCAMENTO     INTEGER NOT NULL,
    ID_PAGAMENTO               SMALLINT NOT NULL,
    DESCRICAO_FORMA_PAGAMENTO  VARCHAR(60) NOT NULL,
    VALOR_PAGO                 DECIMAL(15,2) NOT NULL,
    ID_ORCAMENTO               INTEGER NOT NULL,
    TIPO_FORMA_PAGAMENTO       VARCHAR(13) NOT NULL,
    ORDEM                      CHAR(1) NOT NULL
);


ALTER TABLE PAGAMENTO_ORCAMENTO ADD CONSTRAINT PK_PAGAMENTO_ORCAMENTO PRIMARY KEY (ID_PAGAMENTO_ORCAMENTO);

ALTER TABLE PAGAMENTO_ORCAMENTO ADD CONSTRAINT FK_PAGAMENTO_ORCAMENTO_1 FOREIGN KEY (ID_PAGAMENTO) REFERENCES FORMA_PAGAMENTO (ID_PAGAMENTO);
ALTER TABLE PAGAMENTO_ORCAMENTO ADD CONSTRAINT FK_PAGAMENTO_ORCAMENTO_2 FOREIGN KEY (ID_ORCAMENTO) REFERENCES ORCAMENTO (ID_ORCAMENTO);


CREATE OR ALTER TRIGGER PAGAMENTO_ORCAMENTO_BI FOR PAGAMENTO_ORCAMENTO
ACTIVE BEFORE INSERT POSITION 0
as
begin
  if (new.id_pagamento_orcamento is null) then
    new.id_pagamento_orcamento = gen_id(gen_pagamento_orcamento_id,1);
end


